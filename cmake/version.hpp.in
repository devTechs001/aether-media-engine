// Version information for AETHER Media Engine
// Auto-generated by CMake

#ifndef AETHER_VERSION_HPP
#define AETHER_VERSION_HPP

#include <cstdint>
#include <string_view>

namespace aether {

// Version numbers
constexpr uint32_t VERSION_MAJOR = @AETHER_VERSION_MAJOR@;
constexpr uint32_t VERSION_MINOR = @AETHER_VERSION_MINOR@;
constexpr uint32_t VERSION_PATCH = @AETHER_VERSION_PATCH@;

// Version as single integer
constexpr uint32_t VERSION = VERSION_MAJOR * 10000 + VERSION_MINOR * 100 + VERSION_PATCH;

// Version string
constexpr std::string_view VERSION_STRING = "@AETHER_VERSION_STRING@";

// Codename
constexpr std::string_view CODENAME = "@AETHER_VERSION_CODENAME@";

// Build type
#ifdef NDEBUG
    constexpr std::string_view BUILD_TYPE = "Release";
#else
    constexpr std::string_view BUILD_TYPE = "Debug";
#endif

// Git information
extern const char* GIT_COMMIT_HASH;
extern const char* GIT_BRANCH;
extern const char* BUILD_DATE;
extern const char* BUILD_TIME;

// Build info structure
struct BuildInfo {
    uint32_t version_major = VERSION_MAJOR;
    uint32_t version_minor = VERSION_MINOR;
    uint32_t version_patch = VERSION_PATCH;
    const char* version_string = VERSION_STRING;
    const char* codename = CODENAME;
    const char* build_type = BUILD_TYPE;
    const char* git_commit = GIT_COMMIT_HASH;
    const char* git_branch = GIT_BRANCH;
    const char* build_date = BUILD_DATE;
    const char* build_time = BUILD_TIME;
    const char* compiler = "@AETHER_COMPILER_ID@";
    const char* compiler_version = "@AETHER_COMPILER_VERSION@";
    const char* platform = "@AETHER_PLATFORM@";
    const char* architecture = "@AETHER_ARCHITECTURE@";
};

} // namespace aether

#endif // AETHER_VERSION_HPP
